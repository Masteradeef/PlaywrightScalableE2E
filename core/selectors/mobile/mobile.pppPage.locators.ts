import { Page } from '@playwright/test';
import { BasePPPPageLocators } from '../common/shared.pppPage.locators';

export class MobilePPPPageLocators extends BasePPPPageLocators {
  constructor(page: Page) {
    super(page);
  }

  // Add any mobile-specific locators here if needed
  // Most locators will be inherited from the base class
}














