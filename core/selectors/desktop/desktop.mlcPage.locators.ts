import { Page } from '@playwright/test';
import { BaseMLCPageLocators } from '../common/shared.mlcPage.locators';

export class DesktopMLCPageLocators extends BaseMLCPageLocators {
  constructor(page: Page) {
    super(page);
  }

  // Add any desktop-specific locators here if needed
  // Most locators will be inherited from the base class
}














