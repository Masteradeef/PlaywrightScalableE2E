import { Page } from '@playwright/test';
import { BaseDealerWebsiteWidgetPageLocators } from '../common/shared.dealerWebsiteWidgetPage.locators';

export class DesktopDealerWebsiteWidgetPageLocators extends BaseDealerWebsiteWidgetPageLocators {
  constructor(page: Page) {
    super(page);
  }

  // Add any desktop-specific locators here if needed
  // Most locators will be inherited from the base class
}














