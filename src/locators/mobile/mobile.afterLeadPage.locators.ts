import { BaseAfterLeadPageLocators } from '../base/base.afterLeadPage.locators';
import { Page } from '@playwright/test';

export class MobileAfterLeadPageLocators extends BaseAfterLeadPageLocators {
  constructor(page: Page) {
    super(page);
  }

  // #region Mobile-only locators
  // #endregion
}
